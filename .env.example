# === Required ===

# App secret key for encrypting API keys at rest
# Generate: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
SECRET_KEY=

# NextAuth session encryption
# Generate: openssl rand -base64 32
NEXTAUTH_SECRET=

# === OAuth (at least one required for login) ===

# GitHub OAuth — https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google OAuth — https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# === Optional ===

# LLM API key — users can also set this via the web UI settings page
ANTHROPIC_API_KEY=

# === Production only (docker-compose.prod.yml) ===

# Your domain — must have DNS A record pointing to your server
# DOMAIN=coach.example.com
